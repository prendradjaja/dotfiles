# alias log='e +Sprot'
#
# # TODO do something other than most recent dir
# alias sprot='cd ~/sprot/everlaw && cd $(ls -d */ | tail -n1) && cd $(ls -d */ | tail -n1) && cd $(ls -d */ | tail -n1)'
#
# summary() {
#     for file in *; do
#         echo $file
#         cat $file | grep --color=never SUM: | sed "s/SUM: /  /"
#     done
#     if [ -f "NOTES" ]; then
#         echo
#         cat NOTES
#     fi
# }
# # }
